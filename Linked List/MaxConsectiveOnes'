 int sum=0; // Basically same number of 1's
        int finalAnswer=0;
        for(int i=0;i<nums.size();i++)
        {
            if(nums[i]==1)
            {
                sum++;
            }
            else
            {   
                sum=0;
            }
            finalAnswer=max(sum,finalAnswer);
        }
        return finalAnswer;
